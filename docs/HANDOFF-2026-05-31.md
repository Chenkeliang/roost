# Roost — 夜间进度交接(2026-05-31 → 第二天)

> 目标:"继续完成剩余的工作 … 第二天我要求是个完整的项目包含之前聊好的功能。"
> 本文件是早晨第一眼看的东西:**做了什么、怎么跑、验证状态、还剩什么及为什么没做**。

## 1. 今晚交付了什么(全部 CI 双平台绿)

按设计/路线图(`docs/superpowers/specs/2026-05-31-dashboard-redesign-design.md`)推进,流程严格:brainstorm → spec → writing-plans → subagent-driven,**每个单元 TDD,我逐个独立核验门禁/git scope + 评审 diff 后才推送验 CI**(子代理汇报本会话不可靠,故全部亲验)。

- **P0 止血去假**:status 短路守卫(冷调 24s→9ms)、顶栏对齐、真 hostname、去掉假机器卡(单真卡+诚实空态)、Settings 真链接 + 文档入口。
- **P1 内容优先重构(完成)**:
  - **ADR-0006 `index()` 契约**(便宜索引 vs 显式 `discover()`,按需扫描、主动权在用户)。
  - **五个模块富页**:Projects(按远端主机分组 + [测试]/[保存] + 家目录相对路径)、Packages(解析 Brewfile + 没装 brew 优雅态 + 从本机导入)、Dotfiles、App Config、Aliases & Env。
  - **左侧分组侧栏 IA**(替代顶栏横排)。
- **i18n 中英**:干净的 web i18n 层 + 侧栏 EN/中 切换 + localStorage 记忆(默认 en,旧测试不破)。
- **Settings「Git 远端与同步」面板**:远端 URL、领先/落后、Push/Pull(凭据走你的 git 环境,Roost 不碰)。
- **文档站**更新反映重设计(en + zh-cn)。
- (更早)exec 修复、projects 扫描有界化、25s 缓存、env 模块 + secret op/rbw(ADR-0004)。

**测试**:603 core/cli + 67 web 全绿;`ci`(ubuntu+macOS)与 `docs` 工作流均 success。分支 `feat_p1_mvp`。

## 2. 怎么跑 / 预览

```bash
cd /Users/keliang/MacMove
pnpm install && pnpm -r build
# 仪表盘(图形界面):
pnpm --filter @roost/web build
node packages/cli/dist/index.js serve --web packages/web/dist --repo "$HOME/.local/share/chezmoi"
# 打开 http://127.0.0.1:4317 —— 左侧栏:Overview / Modules(Dotfiles/Packages/Projects/App Config/Aliases & Env)/ Drift / Timeline / Settings;右下角 EN/中 切换。
```
- **Projects**:点 [Scan for git projects] → 你的 ~88 个仓库按主机分组(gitlab.luojilab.com / github.com / code.qschou.com),每行 [Test]/[Save]。
- **Packages**:显示你的 Brewfile(或"未安装 Homebrew");[Import from this Mac]。
- **Aliases & Env**:你迁移进去的别名/env(搜索优先)。
- 文档站:`pnpm --dir website dev`。

## 3. 还剩什么 + 为什么没自主做(等你拍板)

我**刻意没碰**这些——它们要么改 schema(应你审)、要么需要你的账号/破坏性操作,夜间擅自做违背你"考虑架构、不堆内容"的要求:

- **P2 真·多机 / 主从 + Profiles**(`ADR-0005`,proposed):让"同一项在不同机器取不同值/路径""仅工作机纳管某仓库"。**这是 `selection.yaml` schema 改动**,按治理该你审 ADR-0005 后再实现。真·主从机器卡 + 跨机漂移依赖它。
- **env 路径可移植性**(§7A):env 值目前是字面量,`$HOME` 不展开(我们迁移时写死的 `GOPATH=/Users/keliang/go` 在异用户名副机会错)。修它要动安全硬化过的 `generateEnvSh`,与上面的可移植性同属一批,留待一起做/审。
- **P3 发布**:桌面签名(需 Apple Developer 账号)、`inventory/` 清洗(**破坏性 + 需你决定公开**)、文档站部署(需托管)。这些我都没动。

## 4. 诚实说明
- 仪表盘**无假数据**:没有第二台机器就显单卡 + 空态;工具没装(brew/op/rbw)给明确空态不报错。
- 架构未破例:`index()` 可选向后兼容、唯一 exec 出口、不碰凭据/密钥、除 ADR-0006(已接受)外无 schema 改动。
- 路线图勾选见 spec §11;P0/P1/i18n/Git 面板 = 完成,P2/P3 = 上面待办。
